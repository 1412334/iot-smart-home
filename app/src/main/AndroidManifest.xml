<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phuc.iot_smart_home_v2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".components.GlobalSocket"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:noHistory="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.SignupActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.ControlActivity" />
        <activity android:name=".activities.IndexActivity" />
        <activity android:name=".activities.ComponentDetailActivity" />
        <activity
            android:name=".activities.TVRemoteControlActivity"
            android:label="@string/title_activity_remote_control"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.ACRemoteControlActivity"
            android:label="@string/title_activity_acremote_control"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>